
comando backend:

find . -type f \
! -name "package.json" \
! -name "package-lock.json" \
! -name "notasbackend.txt" \
! -name "comando.txt" \
! -name ".dockerignore" \
-exec sh -c '
  for file do
    echo "=============================="
    echo "FILE: $file"
    echo "=============================="
    cat "$file"
    echo
  done
' sh {} + > notasbackend.txt



comando frontend:

find . -type f \
! -name "README.md" \
! -name "package.json" \
! -name "package-lock.json" \
! -name "notas-frontend.txt" \
! -name ".gitignore" \
! -name ".dockerignore" \
-exec sh -c '
  for file do
    echo "=============================="
    echo "FILE: $file"
    echo "=============================="
    cat "$file"
    echo
  done
' sh {} + > notas-frontend.txt




docker logs -f transcendence_backend





arregar cuando se juntan dos fichas distintas en una misma safe cells se pone una encimna de otra en vez de ponerse una alado de otra